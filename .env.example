# wallrus environment variables
# Copy this file to .env and fill in the values.

# ─── API base URL ─────────────────────────────────────────────────────────────
# Empty for web (same-origin relative fetch). Set at runtime on mobile (slice 016).
PUBLIC_API_BASE=
