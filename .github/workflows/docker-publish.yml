name: Build and publish Docker image

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
    paths:
      - "src/**"
      - "static/**"
      - "drizzle/**"
      - "package.json"
      - "bun.lock"
      - "svelte.config.*"
      - "vite.config.*"
      - "tailwind.config.*"
      - "Dockerfile"
      - "docker-entrypoint.sh"
      - ".dockerignore"
      - ".github/workflows/docker-publish.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trigger deploy webhook
        if: github.ref == 'refs/heads/main'
        continue-on-error: true
        env:
          WEBHOOK_USER: ${{ secrets.DEPLOY_WEBHOOK_USER }}
          WEBHOOK_PASSWORD: ${{ secrets.DEPLOY_WEBHOOK_PASSWORD }}
        run: |
          curl -fsS --retry 3 --retry-delay 5 --max-time 30 \
            -X POST \
            -u "${WEBHOOK_USER}:${WEBHOOK_PASSWORD}" \
            https://webhook.tigor.web.id/hooks/deploy-wallrus
