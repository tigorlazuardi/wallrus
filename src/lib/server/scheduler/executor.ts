// One-run orchestrator. Resolves the source, builds a SourceContext, iterates
// items, handles dedup / fan-out / thumbnail / run_history finalize +
// prune-100. Lands as the ingest-pipeline commit.
//
// See `docs/ARCHITECTURE.md` §Ingest pipeline.

export {} // placeholder
